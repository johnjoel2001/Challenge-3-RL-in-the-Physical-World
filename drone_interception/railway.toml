[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["python", "node"]
installCmd = "pip install -r requirements.txt && cd react-demo && npm install"
buildCmd = "cd react-demo && npm run build"

[deploy]
startCommand = "python3 backend/server.py"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
