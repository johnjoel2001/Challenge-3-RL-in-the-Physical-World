[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["python", "node"]
buildCmd = "bash build.sh"

[deploy]
startCommand = "python3 backend/server.py"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
