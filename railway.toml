[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["python", "node"]
buildCmd = "cd drone_interception && pip install -r requirements.txt && cd react-demo && npm install && npm run build"

[deploy]
startCommand = "cd drone_interception && python3 backend/server.py"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
